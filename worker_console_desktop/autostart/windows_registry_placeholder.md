# Windows Registry Startup Placeholder

Future Windows support may add a start-on-login entry under the current user's registry startup location.

This phase does not write registry keys, does not require administrator privileges, and does not install a Windows service.

Planned shape only:

```text
HKCU\Software\Microsoft\Windows\CurrentVersion\Run
```

Current status: planned, not implemented.
