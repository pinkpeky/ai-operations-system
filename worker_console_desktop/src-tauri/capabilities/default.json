{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop window permissions for the Worker Console shell.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
