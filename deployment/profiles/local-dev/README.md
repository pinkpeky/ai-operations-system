# local-dev Profile

Use this profile for local developer validation. It favors localhost URLs, mock providers, and optional Docker dependencies.

It is not a production deployment profile and should not be exposed to the public internet.

