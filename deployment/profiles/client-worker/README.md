# client-worker Profile

Use this profile on the customer machine that runs `worker_client`.

Keep `127.0.0.1:9100` local. Do not expose the worker runtime port to the public internet. Use VPN, Tailscale, or LAN routing for controlled access.

