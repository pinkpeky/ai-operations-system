# server-docker Profile

Use this profile on a server host running Docker Compose. It validates API, browser-worker, PostgreSQL, Redis, Qdrant, workflow routes, task runs, and output artifacts.

This is not Kubernetes, Helm, Terraform, Ansible, or production HA.

